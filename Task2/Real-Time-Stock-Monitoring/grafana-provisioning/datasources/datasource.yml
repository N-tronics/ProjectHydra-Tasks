apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Use the service name defined in docker-compose
    url: http://prometheus:9090
    isDefault: true
    access: proxy
    editable: true
