services:
  hydra-service:
    build: hydra-service/
    image: hyrdra-service-image:latest
    ports:
      - "80:80"
    networks:
      - hydra-net
  exporter:
    build: exporter/
    image: hydra-exporter:latest
    ports:
      - "8000:8000"
    networks:
      - hydra-net
    depends_on:
      - hydra-service

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    networks:
        - hydra-net
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    depends_on:
      - exporter

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    networks:
        - hydra-net
    volumes:
      - ./grafana-provisioning:/etc/grafana/provisioning
      - grafana-storage:/var/lib/grafana
    depends_on:
      - prometheus

networks:
  hydra-net:

volumes:
  grafana-storage:
